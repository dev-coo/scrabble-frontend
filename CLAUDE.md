# CLAUDE.md — Scrabble Frontend

> 작성일: 2026-07-19

이 파일은 Claude가 매 세션 자동으로 읽는 **프로젝트 행동 규칙**입니다.
아래 규칙을 반드시 지켜서 작업하세요.

---

## 0. 이 프로젝트는 무엇인가

- 스크래블(Scrabble) 게임을 **프론트엔드 / 백엔드로 나눠서** 개발합니다.
- 원래 비개발자가 개발자처럼 일하는 방식을 익히려고 시작한 프로젝트였고,
  **그 단계는 끝났습니다.** 게임은 완성됐고 지금은 배포된 서비스입니다.
- 그래서 지금 Claude가 할 일은 **요청한 것을 제대로, 빠르게 만드는 것**입니다.
  작업을 쪼개도록 코칭하지 않습니다.
- 다만 **3티어의 경계는 계속 지킵니다**(규칙 C). 이건 배우려고 둔 장치가
  아니라, 두 저장소가 각자 독립적으로 바뀔 수 있게 하는 실제 구조입니다.

**배포 주소:** https://138.2.127.27.sslip.io

## 1. 이 저장소의 위치: 프론트엔드(표현) 티어

3티어 구조에서 각 티어의 역할:

| 티어 | 역할 | 이 저장소 |
|------|------|-----------|
| **프론트엔드** | 화면/UI, 사용자 입력, 백엔드 API 호출 | ✅ **여기** |
| 백엔드 | 비즈니스 로직, API 제공, DB 접근 | ❌ (별도 저장소) |
| DB/서버 | 데이터 저장 | ❌ (백엔드 담당) |

**프론트엔드는 데이터를 직접 만들지 않습니다. 백엔드에 API로 "요청"해서 받아옵니다.**

## 2. 핵심 규칙 (Claude가 반드시 지킬 것)

### 규칙 A — 요청한 만큼 만든다
- **요청한 범위를 그대로 구현합니다.** 쪼개서 다시 요청하라고 되묻지 않습니다.
- 여러 기능이 섞인 요청이면 그대로 다 만들면 됩니다.
- 다만 **읽는 사람이 따라올 수 있게** 만듭니다 — 파일 하나가 끝없이 길어지면 모듈로 나누고, 화면 조각은 `game/`·`chat/` 처럼 제 자리에 둡니다.

> 예전에는 "한 번에 기능 하나만"이라는 규칙이 있었습니다. 기능을 어떤
> 단위로 쪼개는지 몸으로 익히려는 장치였고, 그 목적은 끝났습니다.
> 지금은 게임이 다 만들어졌고 **속도가 중요한 단계**라 뺐습니다.
> 쪼개는 감각은 규칙 D(커밋 단위)에 남아 있습니다.

### 규칙 C — 티어 경계를 지킨다 (프론트만)
- 이 저장소에서는 **프론트엔드 코드만** 작성합니다. (UI, 화면 상태, 사용자 상호작용)
- **백엔드 로직·DB 쿼리·서버 코드는 작성하지 않습니다.**
- 데이터가 필요해지면:
  1. 프론트가 백엔드에 **기대하는 API 계약**을 정의합니다 — 엔드포인트, HTTP 메서드, 요청 형식, 응답(JSON) 형식.
  2. 이 계약을 `docs/api-contract.md`에 기록합니다.
  3. "이 API 계약을 **백엔드 담당에게 전달**하세요"라고 사용자에게 안내합니다.
  4. 백엔드가 완성되기 전에는 **목(mock) 데이터**로 프론트를 진행할 수 있습니다.
- 이렇게 하는 이유(사용자에게 짧게 설명): 프론트와 백은 **API라는 약속(계약)**으로만 연결되며, 각자 독립적으로 개발할 수 있어야 하기 때문입니다.

**상대 저장소(백엔드)의 코드는 열어보지 않습니다.**
- 백엔드가 어떻게 구현했는지 궁금해도 그 저장소의 코드를 읽지 마세요. GitHub에서 받아오는 것도 포함입니다.
- 필요한 정보는 **스와거(`/docs`) · 계약 문서 · 담당자**에게서 받습니다.
- 이유: 백엔드의 내부 구현에 맞추면 그쪽이 바꾸는 순간 이쪽이 **조용히** 깨집니다. 계약에만 의존해야 양쪽이 독립적으로 바뀔 수 있습니다.

**웹소켓(WebSocket)은 스와거에 나오지 않습니다.**
- OpenAPI 명세가 요청/응답 방식만 다루기 때문에, `@app.websocket()` 으로 만든 경로는 `/docs` 에도 `/openapi.json` 에도 나타나지 않습니다.
- 그래서 웹소켓은 **백엔드 담당이 손으로 쓴 명세서**를 기준으로 작업합니다. 이 저장소의 `docs/ws-contract.md` 가 그 문서입니다.
- 그 문서가 없으면 접속 주소나 메시지 형식을 **추측하지 마세요.** 백엔드 담당에게 요청하면 됩니다.

### 규칙 D — 커밋 단위
- **기능 경계에서 커밋**합니다. (완전히 원자적일 필요는 없고, 기능 하나 정도의 약간 널널한 단위 허용)
- **커밋은 사용자가 확인한 뒤에만** 진행합니다. 절대 자동으로 커밋하지 않습니다.

### 규칙 E — 설명은 짧게
- 대상은 비개발자입니다. 전문 용어는 풀어서 씁니다.
- 다만 **짧게** 씁니다. 한 일을 몇 줄로 요약하면 충분하고, 길게 강의하지 않습니다.
- 설명할 값어치가 있는 것만 짚습니다 — 되돌리기 어려운 일, 나중에 조용히 깨질 만한 선택, 계약이 바뀌는 지점.

## 3. 참고: 프롬프트는 자동 기록됩니다

- 사용자가 입력하는 모든 프롬프트는 `PROMPTS.md`에 타임스탬프·작성자와 함께 자동 기록됩니다. (협업 회고용, 응답은 기록 안 됨)
- 이 파일은 손대지 마세요. 훅이 자동으로 관리합니다.
